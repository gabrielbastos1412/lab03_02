version: '3'
services:
  app:
    image: bastosgabriel156/webacademy:latest
    container_name: webacademydemo
    restart: always
    ports:
      - "80:3000"
    environment:
      NODE_ENV: production